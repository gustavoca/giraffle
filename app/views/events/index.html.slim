- if @events.any? 
	h1 Incoming Events
	- @events.each do |event|
		.panel.radius
			.row
				.large-12.columns
					.panel-primary
						h1= event.date
			.row
				.large-12.columns
					.panel-secondary
						h2= event.event_set.name
			.row
				.large-12.columns
					.panel-secondary
						-	event.organizers.each do |organizer|
							h3= organizer.name
		br
- else
	h1 No Incoming Events!