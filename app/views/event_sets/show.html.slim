h1= @event_set.name

- @event_set.events.each do |event|
	.panel.radius
		.row
			.large-12.columns
				.panel-primary
					h1= event.order
		.row	
			.large-12.columns
				.panel-secondary
					h2= event.date
		.row
			.large-12.columns
				.panel-secondary
					- event.members.each do |member|
						h3= member.name
	br

= link_to 'Back', event_sets_path
